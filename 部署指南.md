## 一、架构与端口说明

```
浏览器 ──► Nginx 容器（托管 Vue3 静态文件，宿主机端口 9527，对外唯一入口）
              │  /api/* 反向代理
              ▼
          FastAPI 容器（Uvicorn, 容器内 8000，不向宿主机/公网发布端口）
              │
              ▼
          SQLite（单文件，挂载到宿主机 ./data/hotel.db）
```

| 用途     | 端口        | 说明                                                    |
| -------- | ----------- | ------------------------------------------------------- |
| Web 前端 | **9527**    | 可在 `.env` 中修改                                      |
| 后端 API | 容器内 8000 | 仅 compose 内网可见，外部无法直连，只能通过前端反代访问 |

- 两个容器均配置 `restart: unless-stopped`，服务器重启后自动拉起。
- SQLite 数据在宿主机 `./data/hotel.db`。

---

## 二、部署前置检查

在服务器上依次确认：

```bash
# 1. Docker 已安装
docker --version          # 建议 20.10+

# 2. Docker Compose 已可用
docker compose version    # v2 语法；若只有旧版 docker-compose 命令，见「六、常见问题」

# 3. 端口未被占用（9527 很少冲突，但仍建议确认）
ss -tlnp | grep 9527      # 无输出即未被占用
```

如服务器开启了防火墙，需放行前端端口：

```bash
# Ubuntu (ufw)
sudo ufw allow 9527/tcp

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-port=9527/tcp
sudo firewall-cmd --reload
```

---

## 三、部署步骤

### 1. 上传项目

克隆项目到本地：

```bash
git clone <你的仓库地址> /opt/hotel
```

### 2. 准备环境变量

```bash
cd /opt/hotel
cp .env.example .env
vi .env
```

`.env` 内容与说明：

```ini
# 对外访问端口（前端 Nginx），默认 9527
WEB_PORT=9527

# JWT 签名密钥
# 生成随机密钥：python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=hotel-training-system-secret-key-change-me
```

**必改项**：`SECRET_KEY` 换成随机字符串（用于登录令牌签名，不改则使用示例默认值）。
**可选项**：`WEB_PORT` 改成其他端口。

### 3. 构建并启动

```bash
cd /opt/hotel
docker compose up -d --build
```

首次构建需要几分钟（拉取 `python:3.11-slim`、`node:20-alpine`、`nginx:alpine` 镜像 + npm install）。完成后确认：

```bash
docker compose ps
# 应看到 hotel-backend 和 hotel-frontend 两个容器 Up

docker compose logs -f
# Ctrl+C 退出查看；backend 日志出现 "Uvicorn running on ..." 即启动成功
```

首次启动会自动完成：建表 + 初始化演示数据（3 个演示账号、4 种房型、20 间客房、历史账单与预订）。

### 4. 验证部署

```bash
# 本机验证
curl http://127.0.0.1:9527/api/health
# 返回 {"status":"ok"} 即后端链路正常

curl -o /dev/null -s -w "%{http_code}\n" http://127.0.0.1:9527/
# 返回 200 即前端页面正常
```

浏览器访问 `http://服务器IP:9527`，用演示账号登录验证：

| 角色   | 用户名      | 密码       |
| ------ | ----------- | ---------- |
| 管理员 | `admin`     | `admin123` |
| 服务员 | `reception` | `123456`   |
| 顾客   | `guest`     | `123456`   |

> 外网无法访问时，按顺序排查：容器状态（`docker compose ps`）→ 防火墙/安全组 → `.env` 端口是否与访问端口一致。

---

## 四、日常运维

### 查看日志

```bash
docker compose logs -f            # 全部日志（实时）
docker compose logs -f backend    # 仅后端
docker compose logs --tail 100    # 最近 100 行
```

### 修改端口 / 配置

编辑 `.env` 后重建容器（数据不受影响）：

```bash
vi .env
docker compose up -d
```

### 数据备份与恢复

```bash
# 备份（建议定期执行，可加入 crontab）
mkdir -p backup
cp data/hotel.db backup/hotel-$(date +%F).db

# 恢复：先停服，再覆盖数据文件，重启
docker compose stop backend
cp backup/hotel-2026-09-22.db data/hotel.db
docker compose start backend
```

一键定时备份（每天凌晨 2 点）：

```bash
crontab -e
# 添加一行：
0 2 * * * cp /opt/hotel/data/hotel.db /opt/hotel/backup/hotel-$(date +\%F).db
```

### 重置演示数据

停服 → 删除数据库文件 → 重启，自动重新初始化种子数据：

```bash
docker compose stop backend
rm data/hotel.db
docker compose start backend
```

### 更新版本（代码变更后重新部署）

```bash
cd /opt/hotel
# 覆盖/拉取最新代码后：
docker compose up -d --build
```

### 停止 / 重启

```bash
docker compose stop       # 停止（数据保留）
docker compose start      # 再次启动
docker compose down       # 停止并删除容器（数据仍保留在 ./data/）
docker compose up -d      # 重新创建并启动
```

---

## 五、访问入口一览

| 入口                | 地址                              | 说明                   |
| ------------------- | --------------------------------- | ---------------------- |
| 系统首页（登录）    | `http://服务器IP:9527/`           | 三种角色统一登录入口   |
| API 文档（Swagger） | `http://服务器IP:9527/api/docs`   | 答辩演示加分项         |
| 健康检查            | `http://服务器IP:9527/api/health` | 返回 `{"status":"ok"}` |

---

## 六、常见问题

**1. 只有旧版 `docker-compose`（v1，带连字符）命令？**
把文档中的 `docker compose` 全部替换为 `docker-compose` 即可，用法一致。

**2. 构建时 npm install 很慢或失败？**
服务器网络问题，可重试 `docker compose up -d --build`；或给构建配置 npm 镜像：修改 `frontend/Dockerfile` 中 `RUN npm install --no-audit --no-fund` 为
`RUN npm install --no-audit --no-fund --registry=https://registry.npmmirror.com`。

**3. 拉取基础镜像很慢？**
配置 Docker 镜像加速器（`/etc/docker/daemon.json` 中添加 registry-mirrors 后 `systemctl restart docker`）。

**4. 容器反复重启？**
查看日志定位：`docker compose logs backend`。最常见原因是 `data` 目录权限问题——确保运行用户对项目目录有写权限（`chmod -R u+rw /opt/hotel`）。

**5. 想把系统挂到域名 + HTTPS？**
在服务器上另装 Nginx/Caddy 做反代，把域名 80/443 的流量转发到 `127.0.0.1:9527` 即可，系统无需任何改动。

---

## 七、部署清单（速查）

```bash
# 1. 上传项目到 /opt/hotel
# 2. 配置环境变量
cd /opt/hotel && cp .env.example .env && vi .env   # 改 SECRET_KEY，可选改 WEB_PORT
# 3. 构建启动
docker compose up -d --build
# 4. 验证
docker compose ps
curl http://127.0.0.1:9527/api/health
# 5. 浏览器访问 http://服务器IP:9527 登录演示账号
```
